version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
      - NEXTAUTH_URL=https://apipg.epay.co.id
      - NEXT_PUBLIC_NEXTAUTH_URL=https://apipg.epay.co.id
